  const moleIndex =  ['.one', '.two', '.three', '.four', '.five', '.six', '.seven', '.eight', '.nine'];

function chooseRandomMole () {
	let manipulatedMoleIndex = momoleIndex[Math.floor(Math.random()*8)]
	let manipulatedMole = document.querySelector("manipulatedMoleIndex")
	// .addClass("hidden");
	console.log(manipulatedMole); // manipulatedMole;
}

chooseRandomeMole();

  // let visible = true;
  // let nextTime = Date.now() + Math.random *1000;
  // function next() {
  //   if (Date.now() > nextTime) {
  //   	chooseRandomMole().addClass(
  //   	"hidden");
  //   }
  //   requestAnimationFrame(next);
  // }
  // next();

  // first randomly chosen time but no longer than a given value

  // second - the randomly chosen mole from the whole population of moles 

